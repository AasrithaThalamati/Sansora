cat > netlify.toml << 'EOF'
[build]
  publish = "."
  command = "npm install && echo 'Build complete'"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
  force = true

[functions]
  directory = ".netlify/functions"
EOF